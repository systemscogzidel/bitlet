from django.conf.urls import url
import views


urlpatterns = [
    
    # url(r'^$', views.login, name="index"),
    # url(r'^$', views.home, name="index"),
    # url(r'^$', views.header, name="index"),
    # url(r'^$', views.sending_confirm, name="index"),

    # url(r'^amount_enter/$', views.amount_enter, name="amount_enter"),
    # url(r'^$', views.home, name="index"),

]